module.exports = function(grunt) {
    grunt.initConfig({
        'billy-builder': {
            title: 'i18n-module'
            jshint: true
        }
    });

    grunt.loadNpmTasks('billy-builder');
};